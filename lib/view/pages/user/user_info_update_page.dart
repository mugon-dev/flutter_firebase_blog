import 'package:flutter/material.dart';

class UserInfoUpdatePage extends StatelessWidget {
  const UserInfoUpdatePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
